# METHOD | Robot.waitForSeconds(Int seconds)
@fast

@using akrobots
@var utils.debug("Executing action: WAIT (seconds={{seconds}})")

@for Int i in list::range(0, seconds)
    @for Int j in list::range(0, 4)
        @var this.pickUpItems()
        @delay 5
    @done
@done
