# METHOD | Script.getRoutine(String name) -> akrobots::Routine
@fast
@using akrobots

@for Routine routine in this.routines
    @if routine.name == name
        @return routine
    @fi
@done

@return null
